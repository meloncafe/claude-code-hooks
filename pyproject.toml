[tool.poetry]
name = "claude-code-hooks"
version = "0.1.0"
description = "Claude Code Hooks for Signal Harmony"
readme = "README.md"
authors = ["Signal Harmony <dev@signal-harmony.dev>"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.14,<4"
rich = "^15.0.0"
sentry-sdk = "^2.67.1"
typer = "^0.27.1"

[build-system]
requires = ["poetry-core>=2.4.1"]
build-backend = "poetry.core.masonry.api"
